1. set up the github (repo)
2. new environment ( conda create -p venv python==3.10 -y)
git init
git add (filename) or . to add all files
git commit -m "message"
git branch -m Main
git remote add origin SSH
git remote -V
git status
git push -u origin main
3. setup.py
4. requirements.txt
5. Src folder and build the package